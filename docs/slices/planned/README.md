# Planned slices

This directory holds approved upcoming work that is not yet an active implementation target. Move a
document to `active/` only after its scope, dependencies, acceptance criteria, and open decisions
are sufficiently agreed. Update the [slice index](../README.md) in the same change.
